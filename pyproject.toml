[project]
name = "jump-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "imagecodecs>=2025.8.2",
    "jump-portrait>=0.0.29",
    "pooch>=1.8.2",
    "zarr>=3.1.3",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]

[tool.uv.sources]
jump-portrait = { path = "../monorepo/libs/jump_portrait", editable = true }


